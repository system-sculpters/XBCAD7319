<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.XBCAD7319" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/secondary</item>
        <item name="colorPrimaryVariant">@color/secondary</item>
        <item name="android:statusBarColor">@color/white</item>

        <!-- PRIMARY-->
        <item name="android:windowBackground">@color/white</item>

        <!-- TEXT-->
        <item name="colorOnPrimary">@color/primary</item>

        <!-- -->
        <item name="ItemBorder">@color/secondary</item>

        <item name="colorItemLayoutBg">@color/white</item>

        <item name="colorItemBorder">@color/itemOnPrimary</item>

        <!-- INPUT STROKE -->
        <item name="whiteItemBorder">@color/inputBgStroke</item>

        <!-- THEME BACKGROUND -->
        <item name="themeBgBorder">@color/themeBg</item>

        <item name="darkerTitle">@color/darkerTitle</item>

        <item name="userHomeImg">@color/itemPrimary</item>

        <item name="thumbTint">@color/textPrimary</item>

        <item name="trackTint">@color/textPrimary</item>
    </style>

    <style name="Theme.XBCAD7319" parent="Base.Theme.XBCAD7319" />

    <style name="DeleteButtonStyle" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:backgroundTint">@color/darkRed</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:drawable">@drawable/button_background</item>
        <item name="colorPrimary">@color/darkRed</item>
    </style>
</resources>