<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <!-- Define background color -->
    <solid android:color="?attr/colorItemLayoutBg" /> <!-- Background color -->

    <!-- Define stroke (border) properties -->
    <stroke
        android:width="1dp"
        android:color="?attr/whiteItemBorder" />
</shape>